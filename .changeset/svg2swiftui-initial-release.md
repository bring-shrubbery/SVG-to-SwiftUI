---
"svg2swiftui": minor
---

Initial release of `svg2swiftui`, a CLI for converting SVG files into SwiftUI `Shape` structs. Usage: `npx svg2swiftui ./icon.svg ./Icon.swift`. Supports `--struct-name`, `--precision`, `--indentation`, and `--usage-comment` flags.
