'use strict';
Object.defineProperty(exports, '__esModule', { value: true });
var is_similar_text_in_array_1 = require('../is-similar-text-in-array');
var list = ['a', 'b', 'c', 'd'];
test('it should return true when text exists in the list', function () {
    expect(
        is_similar_text_in_array_1.isSimilarTextExistInArray(list, 'a'),
    ).toEqual(true);
});
test('it should return false when text does not exists in the list', function () {
    expect(
        is_similar_text_in_array_1.isSimilarTextExistInArray(list, 'e'),
    ).toEqual(false);
});
test('it should return false when list is empty', function () {
    expect(
        is_similar_text_in_array_1.isSimilarTextExistInArray([], 'a'),
    ).toEqual(false);
});
